{
  "name": "@raise/server",
  "version": "0.2.0-alpha.2",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-http://localhost:5173} VALKEY_URL=${VALKEY_URL:-redis://127.0.0.1:6379} tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1120.0",
    "@fastify/cookie": "^11.1.2",
    "@fastify/static": "^10.1.3",
    "@raise/protocol": "workspace:*",
    "@redis/client": "^6.2.1",
    "fastify": "^5.12.1",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0"
  }
}
